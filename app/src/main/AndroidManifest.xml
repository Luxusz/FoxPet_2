<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fhran.foxpet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroMascota">

            <!-- Activity que aparecerá al iniciar la aplicación -->
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".Registro_Usuario" />
        <activity android:name=".RegistroUsuario2" />
        <activity android:name=".RegistroUsuario3" />
        <activity android:name=".PerfilMascota" />
        <activity
            android:name=".EditarPerfilMascota"
            android:parentActivityName=".PerfilMascota" />
        <activity android:name=".PerfilUsuario" />
        <activity android:name=".Lobby" />
        <activity
            android:name=".VacunasAgenda"
            android:label="@string/title_activity_vacunas_agenda"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".VacunaAgregar"></activity>
    </application>

</manifest>